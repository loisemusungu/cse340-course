<%- include('partials/header') %>

<h1><%= title %></h1>

<h2>Project Categories</h2>

<ul>
    <% categories.forEach(category => { %>
        <li><%= category.name %></li>
    <% }) %>
</ul>

<%- include('partials/footer') %>